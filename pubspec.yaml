name: tu_flota
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none'

# ... (rest of version info)

version: 1.0.0+1

environment:
  # VITAL: Dart 3.x requerido
  sdk: '>=3.0.0 <4.0.0' 

dependencies:
  flutter:
    sdk: flutter
  
  # Asistente IA (Generative AI)
  google_generative_ai: ^0.4.7 

  # Backend y Estado
  supabase_flutter: ^2.4.8
  flutter_riverpod: ^2.5.1
  
  # 🗺️ DEPENDENCIAS AÑADIDAS PARA OPENSTREETMAP y RUTAS OSRM
  # 1. Mapa Base (Muestra OSM)
  flutter_map: ^6.1.0 
  # 2. Manejo de Coordenadas (LatLng)
  latlong2: ^0.9.1 
  # 3. HTTP para llamar a la API de rutas OSRM
  http: ^1.1.0 
  # 4. Decodificador de Polilíneas (Para dibujar la ruta en el mapa)
  flutter_polyline_points: ^2.0.0 


  # Otros
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

# ... (rest of flutter section)

flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # ... (rest of assets/fonts sections)